# Lock file configuration to handle legacy peer dependencies
# This ensures consistent builds across environments
package-lock=false

# Use npm ci for faster, reliable builds
fund=false
audit=false

# Handle peer dependency conflicts
legacy-peer-deps=true
